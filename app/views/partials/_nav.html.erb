<nav class="navbar navbar-expand-lg navbar-dark nav-banner-hm">
  <div class="container-fluid">
    <%= link_to 'HM Panel App', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="nav navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_user %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="eventDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Panels & Events
            </a>
            <ul class="dropdown-menu" aria-labelledby="eventDropdown">
              <li><%= link_to 'Schedule (Coming Soon!)', convention_events_path(@convention), class: 'dropdown-item disabled' %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to 'New Panel Application', new_convention_event_application_path(@convention), class: 'dropdown-item' %></li>
              <li><%= link_to 'My Panel Applications', convention_event_applications_path(@convention), class: 'dropdown-item' %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to 'My Panels', convention_events_path(@convention), class: 'dropdown-item' %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
    <div class="navbar">
      <ul class="navbar-nav ms-auto">
        <% if current_user %>
          <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Welcome Back <%= current_user.username %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="profileDropdown">
            <li><%= link_to 'Sign out', destroy_user_session_path, method: 'delete', class: 'dropdown-item' %></li>
            <li><%= link_to 'Profile', profiles_path, class: 'dropdown-item' %></li>
          </ul>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


